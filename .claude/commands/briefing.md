# Briefing / regra de negócio

Execute a skill **`nova-regra-negocio`** (analista funcional).

Não implemente código neste comando — só `BR-*` `planejado` + briefing `BRF-NNN` em `docs/briefings/`, Status `rascunho`, aguardando G1 humano.
