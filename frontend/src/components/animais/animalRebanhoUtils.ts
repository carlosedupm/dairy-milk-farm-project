/** Tooltip em ações de cadastro bloqueadas por baixa (ficha do animal). */
export const ANIMAL_BAIXADO_ACAO_BLOQUEADA_MSG =
  "Animal baixado — edição não permitida";

/** Mensagem na página /animais/:id/editar quando o animal está baixado. */
export const ANIMAL_BAIXADO_EDITAR_BLOQUEADO_MSG =
  "Para alterar o cadastro, reverta a baixa na ficha do animal.";
