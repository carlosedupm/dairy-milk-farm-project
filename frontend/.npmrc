# Registry público apenas — o projeto não usa GitHub Packages (npm.pkg.github.com).
# Evita que Dependabot exija credenciais de registry privado quando a conta/org
# tem npm ligado ao GitHub Packages (@carlosedupm).
registry=https://registry.npmjs.org/
